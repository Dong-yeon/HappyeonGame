{
  "name": "yokairok",
  "private": true,
  "version": "1.0.0",
  "description": "요괴록 (妖怪錄) - 한국 요괴 육성 방치형 게임",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:itch": "vite build && cd dist && zip -r -FS ../yokairok-web.zip . && cd .. && echo '\\n✅ itch.io 업로드용 dist/../yokairok-web.zip 생성 완료'"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
